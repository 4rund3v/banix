import React from "react";
import { Button } from "react-bootstrap";
const InvoiceButton = () => {
  return (
    <Button className="orderInvoice" variant="outline-info">
      Invoice
    </Button>
  );
};

export default InvoiceButton;
